{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20",
    "python": "3.11"
  },
  "steps": {
    "install": {
      "commands": [
        "...",
        "python -m pip install --target .python-packages ytconverter && mkdir -p .python-packages/bin && printf '#!/bin/sh\\nexec python -m yt_dlp \"$@\"\\n' > .python-packages/bin/yt-dlp && chmod +x .python-packages/bin/yt-dlp"
      ]
    }
  },
  "deploy": {
    "aptPackages": ["ffmpeg"],
    "startCommand": "PYTHONPATH=/app/.python-packages PATH=/app/.python-packages/bin:$PATH npm start"
  }
}

