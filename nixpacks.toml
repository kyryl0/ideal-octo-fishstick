[phases.setup]
nixPkgs = ["ffmpeg", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm install --omit=dev", "python -m pip install --user -r requirements.txt"]

[start]
cmd = "npm start"

